using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GeneralPlug : MonoBehaviour
{
    private Image topExtensionCord; //Positions of Image Pictures
    private Image bottomExtensionCord; //Positions of Green Checks - used to indicate which inputs the player has already done
    private float maxTime;
    private int numberOfPlugs; // The number of plugs that spawns
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
